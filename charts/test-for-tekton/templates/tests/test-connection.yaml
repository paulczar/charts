apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-for-tekton.fullname" . }}-test-connection"
  labels:
    {{- include "test-for-tekton.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-for-tekton.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
